name: Development Environment Deploy Process

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Docker Image Build Process
    environment: development
    uses: QuranSunnah/deployment-tools/workflows/build/development.yaml@main
    with:
      ENVIRONMENT: development
      TAG: ${{ github.sha }}
    secrets:
      HOST: ${{ secrets.HOST }}
      USERNAME: ${{ secrets.USERNAME }}
   

    
    